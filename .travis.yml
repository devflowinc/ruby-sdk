language: ruby
cache: bundler
rvm:
  - 2.7
  - 3.0
  - 3.1
script:
  - bundle install --path vendor/bundle
  - bundle exec rspec
  - gem build trieve_client_ruby.gemspec
  - gem install ./trieve_client_ruby-0.3.3.gem
